const permutation = (n, r) => {

    // return ~
}

const combination = (n, r) => {

}

const multipermuatation = (n, r) => {

}

builtinModules.export={
    permutation,
    combination,
    multipermuatation,
}